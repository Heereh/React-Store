import { styled } from 'styled-components';

export const HeroContainer = styled.section`
	display: flex;
	justify-content: center;
	align-items: center;
	max-width: 1400px;
	height: 600px;
	margin: 0 auto;
	position: relative;
	cursor: pointer;
`;
